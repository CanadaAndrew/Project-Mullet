// calendarUtils.ts
export const handleDatesSelected = (selectedDates: string[], pageName: string) => {
    console.log(`Page ${pageName} - Selected dates:`, selectedDates);
    // Perform other actions common to all pages
  };